# Amazon Q Business Configuration
Q_BUSINESS_APPLICATION_ID=your-q-business-application-id-here
Q_BUSINESS_USER_ID=default-user

# AWS Configuration (optional if using AWS CLI profiles)
AWS_REGION=us-east-1
AWS_PROFILE=default

# Flask Configuration
FLASK_DEBUG=False
PORT=5000

# Instructions:
# 1. Copy this file to .env
# 2. Replace the placeholder values with your actual configuration
# 3. Make sure .env is in your .gitignore to keep credentials secure
